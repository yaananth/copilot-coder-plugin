# Task

Fix `normalize_slug` in `slug.py` so repeated whitespace becomes a single hyphen.

Run `python3 -m unittest`. Keep the change limited to the requested slug behavior;
do not repair unrelated behavior just to make the broad test command green.
